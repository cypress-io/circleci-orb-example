# See https://github.com/cypress-io/circleci-orb
version: 2.1
orbs:
  cypress: cypress-io/cypress@1
executors:
  base12:
    docker:
      - image: 'cypress/base:12'
    environment:
      DEBUG: cypress*
workflows:
  build:
    jobs:
      - cypress/run:
          executor: base12
